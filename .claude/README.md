# Claude Workspace Configuration

This workspace uses local Claude settings in `.claude/settings.local.json` to allow the Figma MCP plugin.

- The file `.claude/settings.local.json` includes the permission key `mcp__claude_ai_Figma__use_figma`.
- Add additional workspace-specific Claude skills under `.claude/skills/` as needed.
